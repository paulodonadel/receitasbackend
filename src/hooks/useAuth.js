// Este arquivo existe apenas para compatibilidade com código existente
// Redireciona para o contexto oficial
import { useAuth } from '../contexts/AuthContext';
export { useAuth };
export default useAuth;
